{
  "github_repo": {
    "display_name": "GitHub repository",
    "description": "Repository in owner/repo format, e.g. my-org/support-issues",
    "type": "text",
    "required": true
  },
  "github_assignee": {
    "display_name": "Default GitHub assignee (optional)",
    "description": "GitHub username to assign new issues to, e.g. octocat",
    "type": "text",
    "required": false
  }
}
